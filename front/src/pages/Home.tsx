import { PageContainer } from '@ant-design/pro-components';
import React from 'react';

/**
 * 每个单独的卡片，为了复用样式抽成了组件
 * @param param0
 * @returns
 */

const Home: React.FC = () => {
  return <PageContainer>djafadls;k</PageContainer>;
};

export default Home;
